<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="using:LiveChartsCore.SkiaSharpView.Avalonia"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ConsumptionNotes.Controls.ChartWidget"
             x:Name="Widget">
    <Border
        Margin="10"
        Padding="10"
        CornerRadius="10"
        BorderThickness="1"
        BorderBrush="{StaticResource LightDarkBrush}">
        
        <Grid
            RowDefinitions="Auto,*">
            
            <StackPanel>
                <TextBlock
                    Text="{Binding #Widget.Text}"/>
                <TextBlock
                    Text="{Binding #Widget.AverageValue}"/>
            </StackPanel>
            
            <lvc:CartesianChart
                x:Name="Chart"
                Grid.Row="1"
                ZoomMode="PanX"
                Series="{Binding #Widget.Series}"
                XAxes="{Binding #Widget.XAxes}"
                YAxes="{Binding #Widget.YAxes}"
                LegendPosition="{Binding #Widget.LegendPosition}"
                LegendTextPaint="{Binding #Widget.LegendTextPaint}"/>
            
        </Grid>
        
    </Border>
</UserControl>
